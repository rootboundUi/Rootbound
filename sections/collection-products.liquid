<section class="collection-products">
  <div class="collection-products__container">
    {% if collection.products.size > 0 %}
      <div class="products__grid">
        {% for product in collection.products %}
          {% render 'product-card', product: product %}
        {% endfor %}
      </div>

      {% if paginate.pages > 1 %}
        <div class="pagination">
          {% if paginate.previous %}
            <a href="{{ paginate.previous.url }}" class="pagination__link">← Previous</a>
          {% endif %}

          {% for part in paginate.parts %}
            {% if part.is_link %}
              <a href="{{ part.url }}" class="pagination__link">{{ part.title }}</a>
            {% else %}
              <span class="pagination__current">{{ part.title }}</span>
            {% endif %}
          {% endfor %}

          {% if paginate.next %}
            <a href="{{ paginate.next.url }}" class="pagination__link">Next →</a>
          {% endif %}
        </div>
      {% endif %}
    {% else %}
      <div class="empty-state">
        <p>No products found in this collection.</p>
      </div>
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "Collection Products",
  "settings": [
    {
      "type": "range",
      "id": "products_per_page",
      "label": "Products Per Page",
      "min": 6,
      "max": 48,
      "step": 6,
      "default": 12
    }
  ]
}
{% endschema %}

<style>
/* ================================
   CLEAN PREMIUM COLLECTION GRID
================================ */

.collection-products__container {
  max-width: 1440px;
  margin: 0 auto;
  padding: 2.5rem 1.5rem 5rem;
  background: #faf8f5;
}

/* GRID */
.products__grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(270px, 1fr));
  gap: 2.8rem;
  margin-bottom: 4rem;
}

/* PRODUCT CARD */
.products__grid > * {
  background: #ffffff;
  border-radius: 18px;
  padding: 1.2rem;
  transition: all 0.35s cubic-bezier(.22,.61,.36,1);
  box-shadow: 0 6px 25px rgba(0,0,0,0.04);
  overflow: hidden;
  position: relative;
}

/* Hover highlight (clean, no overlap) */
.products__grid > *:hover {
  transform: translateY(-6px);
  box-shadow: 0 25px 60px rgba(0,0,0,0.08);
}

/* Image */
.products__grid img {
  width: 100%;
  border-radius: 14px;
  transition: transform 0.6s cubic-bezier(.22,.61,.36,1);
}

/* Image zoom only */
.products__grid > *:hover img {
  transform: scale(1.05);
}

/* PRODUCT TEXT */
.products__grid h3,
.products__grid .product-title {
  font-size: 16px;
  font-weight: 500;
  margin-top: 1.2rem;
  margin-bottom: 0.4rem;
  color: #222;
  letter-spacing: 0.2px;
  transition: color .25s ease;
}

/* Subtle text highlight on hover */
.products__grid > *:hover h3,
.products__grid > *:hover .product-title {
  color: #000;
}

.products__grid .price,
.products__grid .product-price {
  font-size: 15px;
  font-weight: 600;
  color: #111;
}

/* PAGINATION */
.pagination {
  display: flex;
  justify-content: center;
  gap: 0.8rem;
  flex-wrap: wrap;
}

.pagination__link,
.pagination__current {
  padding: 0.6rem 1.4rem;
  border-radius: 40px;
  border: 1px solid rgba(0,0,0,0.12);
  text-decoration: none;
  font-size: 14px;
  transition: all .3s ease;
}

.pagination__link {
  background: #ffffff;
  color: #222;
}

.pagination__link:hover {
  background: #f3eee8;
  border-color: #000;
}

.pagination__current {
  background: #111;
  color: #fff;
  border-color: #111;
}

/* MOBILE */
@media (max-width: 768px) {

  .collection-products__container {
    padding: 1.5rem 1rem 4rem;
  }

  .products__grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 1.2rem;
  }

  .products__grid > * {
    padding: 0.9rem;
    border-radius: 14px;
  }

}

</style>
